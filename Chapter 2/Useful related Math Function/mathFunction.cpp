#include <iostream>
#include <cmath>

int main() {

  // some basic math function that are usefull

  double x = 4;
  double y = 10;
  double z;

  // This two can be use without using <cmath>

  // this max function is use to find the which number is greater then the other
  // z = std::max(x, y);
  // this function is used to find which number is less then the other one
  // z = std::min(x, y);

  // down below rest of it are <cmath ones>

  // this is used to give powers to the numbers
  //z = pow(y, x);

  // this is used to find square, no shits sherlock
  // z = sqrt(25);

  // this is used to find the absolute value 
  // z = abs(-4);

  // this used to round of the number;
  // z = round(3.55);

  // this is used to round the number above the value like 
  // z = ceil(3.14);

  // this ised to round the number down the value like
  //z = floor(4.99);

  std::cout << z;

  return 0;
}