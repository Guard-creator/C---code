#include <iostream>

// break = is used to get out of the loop or stop the loop.
// continue is used to skip a step and continue to loop as usully.

int main () {

  /*
  for(int i = 20; i >= 1; i--) {

    if(i % 2) {
      continue;
    }

    std::cout << i << '\n';
  }
  */

 /*
 for(int i = 1; i <= 20; i++) {
    if(i == 13) {
      break;
    }
    std::cout << i << '\n';
 }
 */

  

}